"""
Benchmark suite for factorise.

Run with:
    pytest benchmarks/ --benchmark-only -v
    pytest benchmarks/ --benchmark-only --benchmark-sort=mean
    pytest benchmarks/ --benchmark-histogram
"""
